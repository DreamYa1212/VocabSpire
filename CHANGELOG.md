# 更新日志

## v2.6.3.2 Alpha (2026-06-06)

### ✨ 新功能

- **词库进度独立存储**：每个词库使用独立的进度文件 `_word_progress_{bankId}.json`，各词库进度互不影响
- **自定义按键绑定**：支持自定义提交答案键和下一题/继续键，设置面板中点击按钮即可捕获新按键（Esc 取消）
- **战斗中切词库自动刷新**：战斗中切换词库或词组时，自动重建分组并重新初始化战斗词池
- **GitHub Release 自动发布**：push tag（如 `v2.6.4`）时 CI 自动构建、打包并发布到 Releases

### 🔧 优化

- **智能进度保存**：答题记录按单词所属词库保存，不再依赖当前选中的词库，避免切换词库后数据写错文件
- **启动加载全部进度**：启动时加载所有词库的进度文件，切换词库时历史进度完整保留
- **固定词池仅分组模式生效**：全词库模式下每场战斗从整个词库随机出题，不再锁定固定词池
- **构建依赖独立**：将游戏 DLL 放入仓库 `libs/` 目录，CI 和本地构建不再依赖本地游戏安装路径

### 🐛 修复

- 修复切换词库后分组统计刷新的数组越界崩溃
- 修复设置面板按键绑定捕获 Esc 时误关面板的问题

### 🏗 构建与部署

- 新增 `package.ps1` 本地打包脚本
- CI 改为 `windows-latest` runner
- 打包文件名带版本号（如 `VocabSpire-v2.6.4.zip`）
- 新增 Makefile `package` 目标

### ⚠️ 注意

- SRS 智能调度功能仍在开发中，设置面板已标注
